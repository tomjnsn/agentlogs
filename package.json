{
  "name": "vibeinsights",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter @aei/web dev",
    "build": "bun --filter @aei/web build",
    "start": "bun --filter @aei/web start",
    "db:setup": "bun --filter @aei/web db:setup",
    "db:generate": "bun --filter @aei/web db:generate",
    "db:migrate": "bun --filter @aei/web db:migrate",
    "db:studio": "bun --filter @aei/web db:studio"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "unenv": "^1.10.0"
  }
}
